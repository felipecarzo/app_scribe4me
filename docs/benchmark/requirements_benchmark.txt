# Dependências isoladas para o benchmark Python (MOT-05)
# Instalar em venv separado — não contaminar o venv do legacy
#
# Criar venv:
#   python -m venv docs/benchmark/.venv
#   docs/benchmark/.venv/Scripts/activate   (Windows)
#   pip install -r docs/benchmark/requirements_benchmark.txt
#
# Executar:
#   python docs/benchmark/benchmark_python.py

ctranslate2>=4.0
sentencepiece>=0.2
torch>=2.0
transformers>=4.40
sentence-transformers>=3.0
sacrebleu>=2.4
